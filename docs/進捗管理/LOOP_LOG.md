# LOOP_LOG ／ 自律ループ実行ログ

`/loop` 経由の自律実行の反復ごとの記録。**追記のみ**(過去記録は書き換えない)。
フォーマット・運用方針は [LOOP_CONTRACT.md §7](LOOP_CONTRACT.md#7-loop_logmd-記録テンプレート) を参照。

---

## 反復ログ

<!-- 反復ごとに以下形式で追記。上に新しいものを書く（新→旧の順） -->

## 2026-07-24 反復 #010(B-3 旧ガント画面の権限チェック完全欠如を発見・修復)

- 開始: セッション再開時、ユーザーより「旧ガント画面のprivileged=Trueハードコードを先に確認」との指示
- タスク: B-3(新規発見・最優先) — `project_tasks_gantt.html`対応ルートの権限チェック調査・修復
- 結果: ✅ 完了
- 経緯: B-2(イベント画面の権限修正)と同種のバグパターンがないか、旧ガント画面(`project_tasks_bp.gantt`)側の`privileged=True`ハードコードを確認したところ、`gantt_update_dates`(ドキュメント上「管理職・マスタのみ使用可能」)に**ロールチェックが実装皆無**と判明。test_clientで無関係な一般ユーザーが他人担当タスクの日付を自由に書き換えられることを実証(修正前: status 200で成功)。ユーザー確認の上、即座に修復
- 変更ファイル:
  - `web_app/routes/project_tasks.py`：新設ヘルパー`_can_touch_gantt_task`(マスタ常時可／担当者本人可／管理職・所属長は`can_access_user`判定)を追加。`gantt_update_dates`(+cascade_ids対象にも適用)・`gantt_update_fields`・`gantt_reorder`の3ルート全てにチェックを追加。`gantt_update_dates`にはdocstring通り`is_privileged`ロールチェックも追加。`gantt()`ルートの`render_template`引数`privileged=True`ハードコードを実際の権限値に修正
- 検証:
  - 構文チェック(`ast.parse`)成功
  - test_clientで3ルート全てについて「無関係な一般ユーザー→403」を確認。マスタ・担当者本人による正当操作は引き続き成功することも確認(`gantt_update_dates`は管理職以上限定のため担当者本人でも一般ロールなら403が正しい仕様)
  - テンプレート側`PRIVILEGED`変数が一般ユーザーでfalse、マスタでtrueに正しく出力されることを確認
  - 検証用タスクは全て後片付け済み
- 学び: 「ドキュメントに書かれた権限制限」と「実装」が一致しているとは限らない。特に複数の類似画面（新旧ガント）がある場合、片方で見つかった権限バグパターンはもう片方にも必ず横展開して確認すべき → 既存の`feedback-prototype-security-gap.md`に追記
- 次回: B-1〜B-3の全修正をまとめてユーザーに最終確認の上、コミット
- ブロック要因: —
- 累積進捗: 発見された不具合3件(B-1〜B-3)すべて修正完了

## 2026-07-23 反復 #001(フェーズ0 完了)

- 開始: 06:30 頃
- タスク: 0-6(save_routine の fill_direction 受け取り)、0-7(当週ガード UI)、0-8(構文チェック・動作検証)
- 結果: ✅ 3タスクとも完了、**フェーズ0 全8タスク完了**
- 変更ファイル:
  - `web_app/routes/project_tasks.py` — `save_routine` に `fill_direction` フォーム値の取得・矯正・`save_routine_task` への引き渡しを追加
  - `web_app/templates/schedule.html` — 当週表示時にガントチャート反映ボタンを disabled 化しツールチップで理由表示
  - `web_app/routes/schedule.py` — `weekly` ビューの render_template に `today_monday`(当週月曜日)を追加
- 検証:
  - 変更した4つの .py ファイルすべて `ast.parse` 構文チェック成功
  - 変更した3つのテンプレートすべて Jinja2 ロード成功
  - test_client + DEV DB(`db/web_app_dev.db`)で実データ検証:
    - 次週表示時はガント反映ボタンが有効、当週表示時は disabled + 説明文言が出ることを確認
    - 定例登録で `fill_direction=bottom` を送信 → DB に正しく保存されることを確認
    - top方向定例はスロット先頭(slot_index=0)、bottom方向定例は末尾(slot_index=4)に配置されることをE2Eで確認
    - 当週へのガント反映POSTはガードされ(DB変化なし)、次週への反映は手動入力タスクも含め全クリア→再構築されることを確認
  - GUI確認(ユーザー指示によりフェーズごとに実施): DEV検証用に一時的にポート5099でサーバーを起動(本番相当と思われるポート5000は `pythonw.exe`(Servicesセッション)が保持しておりtaskkill不可・触らず)。test_client経由で実際のレンダリング済みHTMLを検査し、以下を確認して検証後サーバー停止:
    - 週間予定画面：当週表示時は「📊 ガントチャート反映（当週は使用不可）」ボタンがdisabled+ツールチップ付きで表示、次週表示時は通常の有効ボタンが表示
    - タスク管理画面(定例タブ)：詰め方向セレクトボックス(上から/下から)が登録フォームに表示、一覧テーブルにも「下から」等の値が正しく表示
  - GUI不具合: 検出なし
- 学び: **本番ポート(5000)は `pythonw.exe` が Windows サービス(Servicesセッション)として保持しており、通常ユーザー権限の taskkill では停止できない(アクセス拒否)**。DEV検証時は本番ポートに触れず、別ポート(5099等)で都度起動・検証後に停止する運用とする → `feedback-dev-server-port.md` に記録
- 次回: フェーズ1(ガントチャート視覚入力の逆転フロー)の 1-1(gantt_input_test.html の基本動作確定)に着手
- ブロック要因: —
- 累積進捗: フェーズ0 4/7 → 7/7(100%完了)

## 2026-07-23 反復 #002(1-0 planner.py 権限チェック欠如の緊急修復)

- 開始: 07:00 頃(調査エージェントの報告を受けて着手)
- タスク: **1-0(新規追加・最優先)** — `planner.py` の権限チェック欠如の修復
- 結果: ✅ 完了
- 経緯:
  - フェーズ1着手前の調査(Exploreエージェント)で、`base.html`/`project_tasks.html`/`project_tasks_dashboard.html` の「ガントチャート」リンクが**既に全て新方式(`planner_bp.planner`)に切り替わっており**、旧本番画面(`project_tasks_gantt.html`)は直URL打ちでしか到達できない状態と判明
  - 同時に `planner.py` が「試作画面のため役職制限なし」とコメントで自認したまま、`/planner/save` が任意の `user_id` を受け付ける実装になっていることが判明。ユーザーに報告し、フェーズ1着手前に最優先で修復する方針の承認を得た
- 変更ファイル:
  - `web_app/routes/planner.py`
    - モジュールdocstringを「試作」表現から実際の権限モデルの説明に更新
    - `_resolve_target()`：他人のユーザーIDを指定する場合、`is_privileged`+`can_access_user`でのチェックを追加。不許可時は既定値へフォールバック
    - `/save`：`payload.user_id`指定時も同様に`can_access_user`チェックを追加(不許可は403)
    - 新設 `_can_touch_existing(existing)`：既存タスクの更新・削除時、担当者/メンバーへの関与または`can_access_user`での権限チェックを行うヘルパー。既存タスクの更新処理・削除処理の両方に適用
- 検証:
  - 構文チェック(`ast.parse`)成功
  - test_client + DEV DBで以下を確認:
    - 一般ユーザーが他人担当のタスクを更新・削除しようとしても `updated:0`/`deleted:0` で無視される(権限チェックが機能)
    - 本人が自分のタスクを更新 → 正常に反映される
    - マスタ(システム管理者)が任意ユーザーのタスクを削除 → 正常に削除される
    - 管理職が自分の配下(`manager_id`一致)のタスクを更新 → 正常に反映される
    - 管理職が配下外ユーザーのタスクを操作しようとすると、`user_id`指定の時点で403(権限がありません)
  - 検証用に作成したテストタスクは全て後片付け済み(本番相当データへの影響なし)
- 学び: 「試作画面」を謳うコードでも、ナビゲーションが実質本番昇格していれば試作前提のセキュリティの緩さは即座にリスクになる。ロールアウト時はUIの導線変更とサーバー側の権限モデルの整合を必ずセットで確認する必要がある → `feedback-prototype-security-gap.md` に記録
- 次回: フェーズ1の 1-1(`gantt_input_test.html` の基本動作確定：ドラッグ描画・保存の一連動作をGUIで確認)に着手
- ブロック要因: —
- 累積進捗: フェーズ1 0/4 → 1/4(1-0完了)

## 2026-07-23 反復 #003(1-1 ガント視覚入力の基本動作確定)

- 開始: 07:15 頃
- タスク: 1-1(ガント視覚入力の基本動作確定)
- 結果: ✅ 完了
- 実施内容:
  - `gantt_input_test.html` のJS主要関数(`setupDraw`＝新規バー描画、`setupBarDrag`＝移動・伸縮、`saveAll`＝保存ペイロード構築)を静的レビュー
  - `setupDraw` 自体には `editable` チェックが無いが、呼び出し元(L.500 `if (editable) setupDraw(track, row)`)で条件付けされており、`makeRow` の `editable` はサーバー側 `_emit` が計算した値を使うため、1-0で強化したサーバー側権限チェックと多層防御の関係になっていることを確認
  - test_clientで一般ユーザーのガント画面初期表示(200、CSRFトークン埋め込み確認)と、ドラッグ描画→保存に相当するペイロード送信(`POST /planner/save`)による新規タスク作成(`created:1`)をE2Eで検証。検証用タスクは後片付け済み
- 変更ファイル: なし(調査・検証のみ、コード変更不要と判断)
- 検証: 上記test_client検証、GUI(レンダリング済みHTML)確認、いずれも不具合なし
- 学び: なし(既存実装が想定通り動作していることの確認)
- 次回: 1-2(本番ガント画面`project_tasks_gantt.html`への統合方針)、1-3(旧フローとの役割整理)はユーザー判断が必要な設計選択のため、着手前に方針を確認する
- ブロック要因: 1-2/1-3は「テスト画面のまま運用するか」「旧フローを残すか完全に逆転させるか」がユーザー判断待ち
- 累積進捗: フェーズ1 1/4 → 2/4

## 2026-07-23 反復 #004(1-2・1-3 旧ガント画面の扱い方針確定、フェーズ1完了)

- 開始: 07:25 頃
- タスク: 1-2(本番ガント画面への統合)、1-3(既存ガントとの役割整理)
- 結果: ✅ 両方完了(ユーザー判断による)、**フェーズ1 全4タスク完了**
- 経緯: AskUserQuestionで旧画面(`project_tasks_gantt.html`)の扱いを確認 → 「当面は両方残し、旧番はURL直打ち専用のまま」との回答。ナビゲーションは現状(新方式`planner_bp.planner`)を維持し、旧画面はロールバック経路として温存する方針で確定
- 変更ファイル: `docs/進捗管理/改善タスク台帳.md`(方針記録のみ、コード変更なし)
- 検証: 該当なし(方針決定のみ)
- 学び: なし
- 次回: フェーズ2(週間予定への反映・ガント起点)の2-1〜2-3に着手。2-1はフェーズ0で実装した内容の統合確認という位置づけのため、実際にガント→週間予定反映のE2Eフローを再確認する
- ブロック要因: —
- 累積進捗: フェーズ1 2/4 → 4/4(100%完了)

## 2026-07-23 反復 #005(フェーズ2 週間予定への反映、全タスク完了)

- 開始: 07:35 頃
- タスク: 2-1(ガント反映ボタンの完成・統合確認)、2-2(定例同時割当の確認)、2-3(予定時間手入力フローの確認)
- 結果: ✅ 3タスクとも完了、**フェーズ2 全3タスク完了**
- 実施内容:
  - test_client + DEV DBで一連のE2Eを検証:
    1. ガント(`project_task`)にユーザー6担当のタスクを1件、定例を1件登録
    2. `/schedule/import_tasks_and_events`(ガント反映ボタン相当)を実行 → 定例がslot 0、タスクがslot 1に同時配置されることを確認(2-1・2-2)
    3. 反映されたタスク行に`hours_0_am_1=2.5`を手入力して`/schedule/save`で保存 → DBに正しく反映、画面レンダリングにも反映されることを確認(2-3)
  - 検証中、フォームフィールド名を誤って`task_name_0_am_1`で送信し`task_name`が空で保存される事象に遭遇したが、原因はテストコード側の誤り(正しいフィールド名は`task_0_am_1`、`_parse_schedule_form`の仕様通り)。実装バグではないことを確認
  - 検証用データ(project_task・weekly_schedule・routine_schedule)は全て後片付け済み
- 変更ファイル: なし(検証のみ)
- 検証: 上記の通り、ガント→週間予定反映→定例同時配置→時間手入力保存の一連フローが正常動作することをE2Eで実証
- 学び: なし
- 次回: フェーズ3(実績入力の現状維持確認)の3-1に着手。変更不要なら1反復で完了予定。その後フェーズ4(振り返り・新機能)の4-1(既存分析画面の棚卸し)へ
- ブロック要因: —
- 累積進捗: フェーズ2 0/3 → 3/3(100%完了)

## 2026-07-23 反復 #006(フェーズ3 実績入力の現状維持確認、完了)

- 開始: 07:45 頃
- タスク: 3-1(現行実績入力フローの確認)
- 結果: ✅ 完了(変更不要と判断)
- 実施内容: `daily.py`の`daily_save`ルートを確認。`weekly_schedule`(予定)とは独立した`result_task_{slot}_{i}`/`result_hours_{slot}_{i}`フィールドで実績を`daily_result`テーブルへ保存する設計であり、今回のガント反映ロジック変更(フェーズ0〜2)は実績入力に一切影響しないことを確認。test_clientで日次実績画面の予定表示・実績保存をE2E検証(`daily_result`に正しく保存)。検証データは後片付け済み
- 変更ファイル: なし
- 検証: 上記の通り
- 学び: なし
- 次回: フェーズ4(振り返り・新機能)の4-1(既存分析画面の棚卸し：`project_tasks_dashboard.html`と`daily.py`の`comments_review`を調査)に着手
- ブロック要因: —
- 累積進捗: フェーズ3 0/1 → 1/1(100%完了)

## 2026-07-23 反復 #007(4-1 既存分析画面の棚卸し)

- 開始: 07:40 頃(調査エージェントを起動、結果待ちの間ユーザーへ中間報告)
- タスク: 4-1(既存分析画面の棚卸し)
- 結果: ✅ 完了
- 実施内容: Exploreエージェントで `project_tasks_dashboard.html`・対応ルート・`comments_review`・`daily_result`/`weekly_schedule`のJOIN有無を調査
- 重要な発見:
  - `project_tasks_dashboard.html` はプロジェクトタスクの状態管理向け多機能画面で、実績(`daily_result`)とは無関係。グラフ過多が「情報過多」の主因と推測
  - `comments_review`(daily.py L.586-650) の権限スコープ設計(`is_privileged`+`get_accessible_users`)は優秀で応用可能
  - **既に `get_task_plan_vs_actual`(models.py L.2151-2201) という「予定vs実績の差分判定」関数が存在**し、`daily.html`(L.977-1010)でahead/ontrack/behindバッジとして使われている。ただしタスク単位・進捗率ベースであり、ユーザーが求める「日次の時間単位での差分」ではないため、4-2では再設計が必要
- 変更ファイル: `docs/進捗管理/改善タスク台帳.md`(§6.1に調査結果を追記、コード変更なし)
- 検証: 該当なし(調査のみ)
- 学び: 新機能を作る前に類似ロジックの既存有無を必ず確認すべき教訓。今回は`get_task_plan_vs_actual`の発見により4-2の設計方針(ゼロから作らず既存を土台に再設計)が大きく効率化された
- 次回: 4-2(予定vs実績の差分可視化・個人向け)の設計・実装に着手。日次の予定時間(weekly_schedule)と実績時間(daily_result)を突合する新ロジックが必要
- ブロック要因: —
- 累積進捗: フェーズ4 0/4 → 1/4

## 2026-07-23 反復 #008(4-2 予定vs実績の差分可視化・個人向け)

- 開始: 07:55 頃
- タスク: 4-2(予定vs実績の差分可視化・個人向け)
- 結果: ✅ 完了
- 実施内容:
  - `daily_view`(daily.py)は既に`schedule_am`/`schedule_pm`(予定)と`result`(実績)を同一日で取得済みのため、新規APIは不要と判断し`daily.html`への表示追加のみで実現
  - `web_app/templates/daily.html`：予定合計セルに`data-total`属性で数値を埋め込み(`id="schedule-total"`)。実績カードに「予定との差分」行を追加(`id="plan-diff"`、当日表示時のみ)
  - JS `updatePlanDiff(resultTotal)` を新設し、既存の`updateResultTotal()`末尾から呼び出し。差分がプラスなら「前倒し」(緑)、マイナスなら「遅れ」(赤)、0なら「予定通り」(グレー)を表示。実績入力・クリア・繰り越し操作などの全既存呼び出し箇所で自動連鎖
- 変更ファイル: `web_app/templates/daily.html`
- 検証:
  - Jinja構文チェック成功、`sum(attribute='hours')`が辞書要素で正しく動作することを確認
  - test_clientで当日の週間予定に3.0hのタスクを登録し、日次実績画面レンダリング結果に`data-total="3.00"`と`plan-diff`要素が正しく出力されることを確認。検証データは後片付け済み
  - JS実行自体はブラウザ環境が必要なためロジックの静的レビューで担保(単純な減算・条件分岐のみで複雑度は低い)
- 学び: なし
- 次回: 4-3(所属長・管理職向けの部下状況可視化)に着手。`comments_review`の権限スコープ設計を応用する
- ブロック要因: —
- 累積進捗: フェーズ4 1/4 → 2/4

## 2026-07-23 反復 #009(4-3・4-4 部下状況可視化・UI設計、フェーズ4完了・全フェーズ完了)

- 開始: 08:05 頃
- タスク: 4-3(所属長・管理職向けの部下状況可視化)、4-4(振り返り画面のUI設計・情報量の絞り込み)
- 結果: ✅ 両方完了、**フェーズ4 全4タスク完了、改善タスク台帳のフェーズ0〜4が全て完了**
- 実施内容:
  - `web_app/models.py`：新関数`get_daily_plan_vs_actual_for_users(user_ids, date_str)`を追加。複数ユーザーの指定日の`weekly_schedule`予定合計・`daily_result`実績合計・差分(diff_hours)・状態(ahead/ontrack/behind)を一括計算
  - `web_app/routes/daily.py`：新ルート`GET /daily/team-progress`(`team_progress`)を追加。`comments_review`と同じ`is_privileged`+`get_accessible_users`権限スコープパターンを踏襲。遅れが大きい順にソートして表示
  - `web_app/templates/team_progress.html`：新規作成。グラフ0個、テーブル+バッジのみのシンプル構成(4-4の方針を体現)
  - `web_app/templates/base.html`：ナビゲーションドロップダウンに「📆 部下の予定・実績振り返り」リンクを追加
- 変更ファイル: `web_app/models.py`、`web_app/routes/daily.py`、`web_app/templates/team_progress.html`(新規)、`web_app/templates/base.html`
- 検証:
  - 構文チェック(`ast.parse`)成功、Jinja2ロード成功
  - test_clientで、配下メンバー(ユーザー6)の当日予定3h・実績1hを用意し、管理職(ユーザー5)でアクセス→200、氏名・「遅れ」・差分値(-2.0h)が正しく表示されることを確認
  - 一般ユーザーでのアクセス→403(権限拒否)を確認
  - ナビゲーションリンクが`/schedule`画面から正しく表示されることを確認
  - 検証データは後片付け済み
- 学び: なし
- 次回: **改善タスク台帳フェーズ0〜4が全完了**。ユーザーへ完了報告し、コミットの可否・次サイクル(feature_roadmap.mdのF-01〜F-05)への移行を確認する
- ブロック要因: —
- 累積進捗: フェーズ4 2/4 → 4/4(100%完了)。**全フェーズ(0〜4)完了**

## 2026-07-24 反復 #011(フェーズ5：本番リリース向けデータ移行機能の実装)

- 開始: ユーザーより「本番リリースをする、新ガントチャートへスムーズにデータ移行したい」との要望
- タスク: 5-1(移行用Excelエクスポート)、5-2(移行用Excelインポート)、5-3(構文チェック・動作検証)
- 結果: ✅ 3タスクとも完了
- 経緯: 要件をAskUserQuestionで段階的に確認(既存project_taskデータを新ガント構造へ／タスク管理一覧のExcel出力を起点／id一致で更新・空欄で新規)した後、Explore/Planエージェントで既存パターン(`import_brabio_excel`、`_can_touch_existing`、`admin.py`のCSV UPSERT等)を調査。EnterPlanModeで詳細設計を作成しユーザー承認を得てから実装
- 変更ファイル:
  - `web_app/models.py`：新関数`import_migration_excel`(2パス処理：Pass1で全行のadd/update確定・実ID取得、Pass2でparent_task_id解決+循環参照チェック)。既存の`_find_user_by_partial_name`を担当者・メンバー氏名解決に再利用
  - `web_app/routes/project_tasks.py`：新関数`_build_flat_excel_for_migration`(フラットExcel生成)、新ルート`/export-migration`(マスタ限定)
  - `web_app/routes/planner.py`：新ルート`/import-excel`(マスタ限定)。`logger`未定義だったため追加。`planner()`に`is_master`変数を追加
  - `web_app/templates/project_tasks.html`：タスク一覧カードヘッダーに「📥 タスク移行Excel出力」ボタン(`is_master`時のみ)
  - `web_app/templates/gantt_input_test.html`：ヘッダーに「📤 Excel取込」ボタン+折りたたみ式アップロードフォーム(`is_master`時のみ)
- 検証:
  - 構文チェック(`ast.parse`)・Jinja2ロード、全ファイル成功
  - test_clientで検証用の親子タスク(親=メンバー新名洋幸、子=担当者亀川典子)を作成 → マスタでエクスポート → 列構成(id/parent_task_id/task_name/担当者1/担当者2/開始日/終了日/状態/進捗%/メンバー)を確認
  - エクスポートしたExcelを編集(子タスクの進捗・状態変更、新規子タスク行追加)→インポート実行 →「1件追加/更新」フラッシュメッセージ、DB側で進捗反映・新規タスクが正しい親に紐付くことを確認
  - 一般ユーザー・管理職(マスタ以外)でエクスポート→403、インポート→flash警告で拒否・DB不変を確認
  - 循環参照(親を自分の子に設定)→エラー記録し親設定をスキップ(親なしにフォールバック)、存在しない担当者名→エラー記録し処理継続、を確認
  - GUI面：マスタのみボタン表示、一般ユーザーには非表示を確認
  - 検証用データは全て後片付け済み
- 学び: 既存の`admin.py`CSVインポート機能に`project_task`向けUPSERTロジックが既にあったが、全テーブル対象のDBバックアップ/リストア用途で権限チェックも粗く、今回の限定用途には転用せず専用実装を選んだ判断が正しかった。目的の異なる既存機能を無理に転用するより、パターン(2パス処理・ファイルアップロード受け取り・氏名解決)だけ再利用して専用実装する方が安全な場合がある
- 次回: ユーザーに実装完了を報告し、ブラウザでの実機確認を依頼。コミットの可否を確認
- ブロック要因: —
- 累積進捗: フェーズ5 0/3 → 3/3(100%完了)

## 2026-07-24 反復 #012(フェーズ0〜5の変更をコミット)

- 開始: ユーザーより「まずコミットしてから次へ」との指示
- タスク: 未コミットだったフェーズ0〜5全ての変更をコミット
- 結果: ✅ 完了
- 実施内容:
  - 不要ファイル(`dev_server.log`・`dev_server_5099.log`・`web_app/web_app_dev.db`(0バイトの誤作成ファイル))をユーザー確認の上削除
  - `.gitignore`に`*.log`を追加
  - `loop_archive_salessupport/`(別プロジェクトのアーカイブ)はユーザー判断で今回のコミット対象外とし、untrackedのまま維持
  - `docs/進捗管理/`(LOOP_CONTRACT.md・LOOP_LOG.md・改善タスク台帳.md)、`web_app/templates/team_progress.html`を含む16ファイルをコミット(e61bd36)
- 変更ファイル: なし(整理・コミットのみ)
- 検証: `git status`でクリーンな状態(loop_archive_salessupportのみuntracked)を確認
- 学び: なし
- 次回: LOOP_CONTRACT §2.2の優先順位に従い、`feature_roadmap.md`のF-01(月間計画)以降へ着手。ただしF-01〜F-05はLOOP_CONTRACTのミッション対象外(現ミッションは改善タスク台帳のフェーズ0〜4/5)のため、着手前にユーザーへLOOP_CONTRACTのミッション更新要否を確認する
- ブロック要因: —
- 累積進捗: フェーズ0〜5 全21タスク完了・コミット済み
